apiVersion: v1
kind: PersistentVolume
metadata:
  name: playwright-pv

spec:
  capacity:
    storage: 2Gi

  accessModes:
    - ReadWriteOnce

  storageClassName: playwright-storage

  persistentVolumeReclaimPolicy: Retain

  hostPath:
    path: /mnt/playwright-artifacts