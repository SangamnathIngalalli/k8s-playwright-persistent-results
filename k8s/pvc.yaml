apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: playwright-results-pvc

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: playwright-storage

  resources:
    requests:
      storage: 2Gi